# gst-invoice

GST Invoice management system built with Laravel.

## Setup
- Clone the repository
- Run `composer install`
- Copy `.env.example` to `.env`
- Configure database
- Run `php artisan key:generate`
- Run `php artisan migrate`
- Start server with `php artisan serve`